package service;

import model.Authtoken;
import model.Person;

/**
 * Returns the single Person object with the specified ID
 */

public class personID {

    /**
     * Returns the single Person object with the specified ID
     * (if the person is associated with the current user). The current user is determined by the provided authtoken.
     * @param personID the ID used to identify the user to call this on
     * @return the specific person connected to the ID
     */

    public Person personID(String personID){
        return null;
    }


}

//    Returns the single Person object with the specified ID
//        (if the person is associated with the current user). The current user is determined by the provided authtoken.